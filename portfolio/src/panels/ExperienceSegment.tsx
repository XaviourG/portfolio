import { ReactElement } from "react";

const ExperienceSegment = (): ReactElement => {
  return <div style={{
    display: 'flex',
    width: '100%',
    backgroundColor: 'red',
    height: '80vh'
  }}>
    navigation bar
  </div>
}

export default ExperienceSegment;
