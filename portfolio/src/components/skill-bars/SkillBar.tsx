import { ReactElement } from "react";
import { MainSkill } from "./Skillset";

interface Props {
  skill: MainSkill
}

const SkillBar = ({
  skill
}: Props): ReactElement => {

}

export default SkillBar;
